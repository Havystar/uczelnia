class GameScreen extends Screen {
  constructor() {
    super();
    this.map = new Array();
  }

  update() {}
  draw() {}
  onClick(x, y, buttons) {
    super.onClick(x, y, buttons);
  }
  onKeyDown(key) {}
  onKeyUp(key) {}
}
